{
  "$schema": "https://raw.githubusercontent.com/anthropics/claude-code/main/schemas/settings.json",
  "description": "Autonomous Orchestration Ecosystem - Hooks for agent evolution management",

  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Task",
        "command": "echo '[Orchestrator] Before spawning sub-agent: Have you scanned pool/ and evaluated coverage? New/integrated agents should be saved to pool/ with skill sheets in manifests/.'"
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Task",
        "command": "echo '[Orchestrator] Sub-agent completed. Update manifests/{agent}.yaml with: usage_count++, success_rate, last_used. Consider elite promotion if success_rate >= 80% and usage_count >= 5.'"
      },
      {
        "matcher": "Write",
        "command": "echo '[Orchestrator] File written. If this is a new agent definition in pool/, ensure corresponding skill sheet exists in manifests/.'"
      }
    ]
  },

  "permissions": {
    "allow": [],
    "deny": []
  },

  "env": {}
}
